// Не умирает по-хорошему: SIGTERM ловит и игнорирует. На нём проверяется, что
// после отсрочки прилетает SIGKILL, а обёртка не ждёт зависшего провайдера.
// `export {}` — чтобы файл был модулем: иначе tsc падает TS1375 на await
// верхнего уровня.
export {}

process.on("SIGTERM", () => {})
await new Promise(() => {})
